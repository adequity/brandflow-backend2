# 데이터베이스 설정
DB_DIALECT=sqlite
DB_STORAGE=./database.sqlite

# 프로덕션에서는 PostgreSQL 사용
# DATABASE_URL=postgresql://username:password@host:5432/database

# 서버 설정
PORT=5000
NODE_ENV=production

# CORS 설정 (Netlify 도메인)
FRONTEND_URL=https://singular-peony-ea66d2.netlify.app